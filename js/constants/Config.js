import {HOUSE_FRONT} from "../../content/places/house/houseFront";
// Languages

// This are all currently supported languages in this project
export const SUPPORTED_LANGUAGES = ['en'];
// If no language is selected, this language is the preselected
export const DEFAULT_LANGUAGE = 'en';
// Defines the first place that is shown when the game starts
export const START_PLACE = 'places.house.houseFront';
// Defines the events that take place a the start of the game
export const START_EVENTS = [];
// Minimum value of a character stat
export const MIN_STAT_VALUE = 0;
// maximum value of a character stat
export const MAX_STAT_VALUE = 100;