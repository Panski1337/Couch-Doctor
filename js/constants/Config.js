// Languages

// This are all currently supported languages in this project
export const SUPPORTED_LANGUAGES = ['en'];
// If no language is selected, this language is the preselected
export const DEFAULT_LANGUAGE = 'en';
